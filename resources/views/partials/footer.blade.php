<footer>
    <div class="container">
        <p class="text-muted">
            <span class="pull-left">
                {{-- &copy; {{ date("Y") }} <a href="{{ url('/') }}">{{ config('params.name') }}</a> --}}
            </span>
            <span class="pull-right">
                {{-- Powered by: <a href="{{ url(config('params.developer_url', '#')) }}">{{ config('params.developer_name', 'Anonymous') }}</a> --}}
            </span>
        </p>
    </div>
</footer>